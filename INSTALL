http://www.phys.ethz.ch/~franklin/Projects/dphys-swapfile/INSTALL
copyright ETH Zuerich Physics Departement, use under either BSD or GPL license
author Neil Franklin, last modification 2004.07.29

To install/config/use dphys-swapfile do the following:


Source install
==============

1. Download and unpack the .tar.gz archive file from:

   http://www.phys.ethz.ch/~franklin/Projects/dphys-swapfile.tar.gz or
   http://www.phys.ethz.ch/~franklin/Projects/dphys-swapfile-<date>.tar.gz
     where there are none at present, use the "current" above

   make
   make install


Alternatively:

1. add to your  /etc/apt/sources list:

   deb-src http://debian.ethz.ch/pub/debian-local woody/local main contrib non-free

2. Fetch it with  apt-get source dphys-swapfile


Binary install
==============

1. Download and unpack the .deb package file from:

   http://www.phys.ethz.ch/~franklin/Projects/dphys-swapfile_<date>-1_all.deb
     where last milestone/release is <date>=20040611.145856

2. Install it with  dpkg --install dphys-swapfile_<date>-1_all.deb


Alternatively:

1. add to your  /etc/apt/sources list:

   deb http://debian.ethz.ch/pub/debian-local woody/local main contrib non-free

2. Install it with  apt-get install dphys-swapfile


Configuration
=============

1. install this software
2. set up the config file (system wide /etc/default/dphys-swapfile or
   /etc/dphys-swapfile or both) if you dislike the defaults
   - They should be good enough for most sites
3. run the dphys-swapfile script at startup:
   - /sbin/dphys-swapfile setup - if you want to auto-resize when RAM changes
   - /sbin/dphys-swapfile swapon - allways
   and shutdown:
   - /sbin/dphys-swapfile swapoff - allways
